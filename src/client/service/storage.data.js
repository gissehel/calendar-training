export const dataMapping = {
    /*
    'state.auth.token': {
        path: ['auth'],
        key: 'token',
        formatGet: (value) => value || undefined,
    },
    'state.auth.user': {
        path: ['auth'],
        key: 'user',
        formatGet: (value) => value || undefined,
    },
    'state.auth.rights': {
        path: ['auth'],
        key: 'rights',
        formatGet: (value) => {
            let arrayValue = [...value];
            return arrayValue.length > 0 ? arrayValue.join(',') : undefined;
        },
        formatSet: (value) => new Set((value === undefined || value === '' || value === null) ? [] : value.split(',')),
    },
    */
};

